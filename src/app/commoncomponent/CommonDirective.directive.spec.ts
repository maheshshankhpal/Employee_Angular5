/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CommonDirectiveDirective } from './CommonDirective.directive';

describe('Directive: CommonDirective', () => {
  it('should create an instance', () => {
    const directive = new CommonDirectiveDirective(this);
    expect(directive).toBeTruthy();
  });
});
